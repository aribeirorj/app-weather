import React from 'react';

import { Search } from '../../../components';

export default {
  title: 'app-Weather/Components/Search',
  component: Search,
};

const Template = () => <Search />;

export const SearchView = Template.bind({});
SearchView.args = {};
