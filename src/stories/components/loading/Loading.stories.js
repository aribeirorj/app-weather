import React from 'react';

import { Loading } from '../../../components';

export default {
  title: 'app-Weather/Components/Loading',
  component: Loading,
};

const Template = (args) => <Loading {...args} />;

export const LoadCard = Template.bind({});
LoadCard.args = {};
